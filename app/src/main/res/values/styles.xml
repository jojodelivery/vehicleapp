<resources>

    <!-- Base application theme. -->
    <style name="JojoTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@color/dashboard_cell_blue</item>
    </style>
    <style name="JojoTheme.ActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:textStyle">bold</item>
        <item name="android:actionBarSize">80dp</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:background">@color/dashboard_icon_golden_yellow</item>
    </style>

</resources>
